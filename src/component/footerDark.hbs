{{#with data}}
<footer class="footer">
    {{#with footer}}
    <nav class="footer_nav">
        {{#each pages}}
        <a href="{{path}}" class="footer_link">{{name}}</a>
        {{/each}}
    </nav>
    <span class="footer_copyright">{{copyright}}</span>
    {{/with}}
</footer>
{{/with}}